import Container from '@/components/layout/container';

const GoalsPage = () => {
  // TODO add goals to context coupled to player, save goals in local storage, add sidebar for adding goals on each page?, user can add, edit, delete, check off goals, add shared goals for group? would need to use an external storage
  return (
    <Container>
      <h2>Goals Page</h2>
    </Container>
  );
};
export default GoalsPage;
